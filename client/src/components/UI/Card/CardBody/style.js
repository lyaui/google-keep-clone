import styled from 'styled-components/macro';

export const SCardContent = styled.div`
  padding: 12px;

  p {
    color: rgba(0, 0, 0, 0.4);
    font-size: 24px;
    margin: 0;
    margin-top: 8px;
  }
`;
